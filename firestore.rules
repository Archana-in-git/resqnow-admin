rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TESTING MODE: Allow all authenticated users full write access
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}